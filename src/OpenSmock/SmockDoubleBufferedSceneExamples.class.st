"
This class provides interactive examples that showcase elements using Smock's double-buffered rendering system.

Its primary purpose is to demonstrate and test visual elements within a Smock graphical scene.

Responsibilities:
- Create and display example scenes using double-buffered Smock elements.

Available examples:
- Works with elements such as SmockDoubleBufferedSquareMapBuildingExampleElement.
- Uses the Smock and Bloc frameworks to layout and render visual components.

Usage:
	SmockDoubleBufferedSceneExamples exampleSquareMap
		=> Opens a window displaying a double-buffered square map scene

"
Class {
	#name : #SmockDoubleBufferedSceneExamples,
	#superclass : #Object,
	#category : #'OpenSmock-Buffer'
}

{ #category : #'see class side' }
SmockDoubleBufferedSceneExamples >> seeClassSide [ 
]
